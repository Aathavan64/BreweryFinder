import styled from 'styled-components';

export const StyledCustomQuery = styled.div`
    background: tomato;
    display: flex;
    flex-direction: column;

    .abc {
        background: green;
    }
`;